package model;

import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;
import java.util.List;

public class Main {
    public static void main(String[] args){



         /*
        EntityManagerFactory emf = Persistence.createEntityManagerFactory(  "gep-mysql");
        EntityManager em = emf.createEntityManager();


        Machines mac = new Machines();
        mac.setId(1);
        mac.setMotor("22/15");
        mac.setFloor_space("2,300 x 1,732");
        mac.setName("LB 3000 EXII");
        mac.setTools_count(4);
        mac.setWeight(67);
        em.getTransaction().begin();
        em.persist(mac);
        em.getTransaction().commit();

        Tools tool = new Tools();
        Gepek gep = new Gepek();
        gep.setId(1);
        gep.setNev("NagyGep");
        gep.setSuly(3);
        gep.setSzerszamok_db(5);

        tool.setToolId(2);
        tool.setName("Mining tool rxtl 312");
        tool.setCondition("New");
        tool.setWeight(5);
        tool.setMaterial("Steel");
        tool.setAbrasion(3);
        tool.setGrade("YG9");
        tool.setMachineId(2);


        em.getTransaction().begin();
        em.persist(tool);
        em.getTransaction().commit();
        System.out.println(tool.getName());
            */


    }



}

